import { extendTheme } from '@chakra-ui/react'

const theme = {
  fonts: {
    heading: `'Roboto', sans-serif`,
    body: `'Lora', -apple-system, 'Times New Roman', serif`,
  },
}

export default extendTheme(theme)